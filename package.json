{
  "name": "easygenerator-auth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run build && node apps/server/dist/main",
    "dev": "turbo run build && turbo run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "apps/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.13.3"
  },
  "dependencies": {
    "client": "^0.0.0",
    "react-router-dom": "^6.23.1"
  }
}
